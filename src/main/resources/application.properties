spring.application.name=featureflags

# ===============================
# PostgreSQL (ENV BASED)
# ===============================
#spring.datasource.url=${DB_URL}
#spring.datasource.username=${DB_USER}
#spring.datasource.password=${DB_PASS}

spring.datasource.url=jdbc:postgresql://aws-1-ap-northeast-1.pooler.supabase.com:5432/postgres
spring.datasource.username=postgres.fvzqahxuottrauafkjut
spring.datasource.password=switchtcsjldi
spring.datasource.driver-class-name=org.postgresql.Driver
#spring.config.import=optional:file:.env



# ===============================
# JPA / Hibernate
# ===============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.time_zone=UTC
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# ===============================
# Hikari (IMPORTANT for Supabase)
# ===============================
spring.datasource.hikari.maximum-pool-size=3
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.max-lifetime=180000
spring.datasource.hikari.connection-timeout=30000

# ===============================
#for health checkpoint
management.endpoints.web.exposure.include=health


# ===============================

# ===============================
# Server
# ===============================
server.port=8080

# ===============================
# Redis Config
# ===============================

spring.data.redis.host=localhost
spring.data.redis.port=6379



#logging.level.org.springframework.boot.context.config=DEBUG
